---
status: planned
depends_on: [20]
wave: 8
skills: [post-deploy-qa]
verify: [smoke, user]
reviewers: []
teammate_name: post-deploy
---

# Task 21: Post-deploy verification (deliberate-break + first recursive self-merge)

## Required Skills
Before starting, load: /skill:post-deploy-qa

## Description
Execute the Agent Verification Plan from tech-spec §6 against the live VM. Two
non-negotiable proofs:
1. **Deliberate-break test:** deliberately corrupt a fabric subsystem; confirm
   `fabric-safe-mode rollback` returns the system to last-known-good and an ops banner
   fires.
2. **First recursive self-merge:** drive a real, non-trivial PR in `mnemonic-loop`
   through the smoke-gate and merge it via the fabric itself.

Sign the post-deploy attestation only on full success. Anchors tech-spec §4 Wave 8 T8.3,
§6 AVP; user-spec AC36, AC37, §8.5.

## What to do
- Execute the 8 AVP steps from tech-spec §6 sequentially.
- For each step, capture evidence (curl output, Telegram screenshot via MCP, journalctl
  excerpt) into `work/coding-fabric/logs/tasks/post-deploy-qa.json`.
- Run the deliberate-break drill: corrupt `state.json`; invoke `fabric-safe-mode rollback`
  via SSH; verify services back; verify ops banner; verify the corrupted state did not
  persist.
- Drive a real `mnemonic-loop` PR (e.g., minor watchdog improvement) through the smoke-gate
  and merge it via the fabric.
- Trigger Mnemonic MCP attestation only after both proofs succeed.

## Acceptance Criteria
- [ ] All 8 AVP steps executed with evidence captured.
- [ ] Deliberate-break rollback drill succeeded; ops banner observed.
- [ ] First recursive self-merge happened end-to-end via the fabric.
- [ ] Post-deploy attestation signed; DAG now has 5 linked nodes for this feature.
- [ ] `mnemonic_recall --feature=coding-fabric` returns the full lineage.

## Context Files
- [tech-spec.md](../tech-spec.md) — §6 AVP, §4 Wave 8 T8.3
- [user-spec.md](../user-spec.md) — AC36, AC37, §8.5

## Verification Steps
**Smoke:**
- `mnemonic_recall --feature=coding-fabric` returns 5 nodes with parent chain matching §2.6
- `mnemonic_verify --feature=coding-fabric` reports integrity ok
- AVP report shows pass for all 8 steps
- The non-trivial `mnemonic-loop` PR merge commit exists and was signed by the bot
  account, not the operator

**User:**
- Confirm via Telegram that the merge happened from your phone with no SSH.
- Confirm the deliberate-break drill's ops banner was visible during the test.

## Details
**Files:** none modified directly; this task captures evidence into
`logs/tasks/post-deploy-qa.json`.

**Dependencies:** Task 20 (deploy complete).

**Edge cases:**
- If the deliberate-break drill fails to recover within 5 minutes — abort, restore
  manually, open a critical remediation task; do NOT proceed to attestation.
- If the recursive self-merge happens to land in a buggy state — last-known-good must
  hold from before; revert via safe-mode.

**Implementation hints:**
- Use Telegram MCP to grab transcripts and Playwright MCP to grab Kaneo screenshots as
  AVP evidence.
- Capture absolute timestamps; the post-deploy attestation includes the AVP report hash.

## Reviewers
(none — this is post-deploy verification; auditors run after if needed)

## Post-completion
- [ ] Write final report to `work/coding-fabric/decisions.md`
- [ ] Move feature to `work/completed/coding-fabric/` via the `/done` skill once approved.
