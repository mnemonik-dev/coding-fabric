# ruflo global configuration (generated by Ansible role)
# Ref: tech-spec §2.4

log_level: "{{ ruflo_global_defaults.log_level }}"
strict_mode: {{ ruflo_global_defaults.strict_mode | lower }}
timeout_seconds: {{ ruflo_global_defaults.timeout_seconds }}

# Per-topic configs are rendered separately in topics/*.yml
