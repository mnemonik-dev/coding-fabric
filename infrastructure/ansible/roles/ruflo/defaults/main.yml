---
# Canonical per-topic configuration matrix (§2.4, tech-spec)
# Used to render all 8 per-topic config files and validate on-disk state
ruflo_version: "0.20.0"  # Pinned upstream version

# Installer checksum (SHA256) for supply-chain security verification
# Default FAIL-FAST: operator MUST override after verifying upstream hash
# To get hash: curl -sL https://install.ashwinchat.ai/ruflo | sha256sum
ruflo_installer_sha256: "PLACEHOLDER_VERIFY_UPSTREAM_AND_OVERRIDE"

ruflo_topic_matrix:
  - topic: core
    engine: claude-code
    autopilot: "off"
    aidefence: "off"
    rag_memory: "on"
    mnemonic_mode: "local"
    memory_namespace: "mnemonic-core"

  - topic: mcp
    engine: claude-code
    autopilot: "off"
    aidefence: "off"
    rag_memory: "on"
    mnemonic_mode: "local"
    memory_namespace: "mnemonic-mcp"

  - topic: wasm
    engine: claude-code
    autopilot: "off"
    aidefence: "on"
    rag_memory: "on"
    mnemonic_mode: "local"
    memory_namespace: "mnemonic-wasm"

  - topic: demo-client
    engine: codex
    autopilot: "on"
    aidefence: "on"
    rag_memory: "on"
    mnemonic_mode: "local"
    memory_namespace: "mnemonic-demo-client"

  - topic: docs
    engine: claude-code
    autopilot: "on"
    aidefence: "on"
    rag_memory: "on"
    mnemonic_mode: "local"
    memory_namespace: "mnemonic-docs"

  - topic: loop
    engine: claude-code
    autopilot: "off"
    aidefence: "on"
    rag_memory: "on"
    mnemonic_mode: "local"
    memory_namespace: "mnemonic-loop"

  - topic: protocol-qa
    engine: claude-code
    autopilot: "off"
    aidefence: "on"
    rag_memory: "off"
    mnemonic_mode: "full"
    memory_namespace: "mnemonic-qa"

  - topic: ops
    engine: claude-code
    autopilot: "off"
    aidefence: "on"
    rag_memory: "on"
    mnemonic_mode: "local"
    memory_namespace: "mnemonic-ops"

# Global defaults applied to all topics
ruflo_global_defaults:
  log_level: "info"
  strict_mode: true
  timeout_seconds: 300
