---
galaxy_info:
  role_name: tailscale
  namespace: fabric
  description: Install tailscaled, authenticate to tailnet, and register tailnet IP fact
  company: Mnemonic Protocol
  author: Mnemonic Development
  license: proprietary
  min_ansible_version: '2.16'
  platforms:
    - name: Ubuntu
      versions:
        - '24.04'
  galaxy_tags:
    - infrastructure
    - networking
    - vpn
    - tailscale

# Collection requirements (community.general, ansible.posix) are installed
# externally via `ansible-galaxy collection install -r requirements.yml`.
# See base/meta/main.yml for full rationale.
dependencies: []
