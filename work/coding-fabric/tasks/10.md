---
status: dropped
depends_on: [02]
wave: 3
skills: [ansible-automation]
verify: [smoke]
reviewers: [code-reviewer, security-auditor]
teammate_name: ansible-ruflo
---

# Task 10: Ansible role `ruflo` — DROPPED 2026-05-20

> **DROPPED 2026-05-20.** ruflo removed end-to-end after operator
> critical-evaluation (inherited-without-critical-eval cleanup). ruflo's
> features (swarm orchestration, plugin tools, AgentDB memory, observability)
> overlap with what molyanov-ai-dev + Claude Code Agent tooling already
> provide. Files under `infrastructure/ansible/roles/ruflo/` deleted; this
> task file is preserved so numbering (and git-history continuity) stays
> stable. See `work/coding-fabric/decisions.md` Round 3 section for full
> rationale.

---

Original task body kept below for historical reference only.

# Task 10: Ansible role `ruflo`

## Required Skills
Before starting, load: /skill:ansible-automation

## Description
Install ruflo full CLI and emit per-topic configs implementing the cohabitation matrix
from tech-spec §2.4 (autopilot/aidefence/rag-memory toggles, `MEMORY_NAMESPACE` pinning).
Anchors user-spec AC25–AC28, tech-spec D17.

## What to do
- `roles/ruflo/tasks/main.yml`:
  - install ruflo via upstream installer (pin version)
  - render `~/.fabric/ruflo/global.yml` from template
  - render 8 per-topic files `~/.fabric/ruflo/topics/{topic}.yml` from template + matrix var
  - install `validate_matrix.py` and invoke it to assert on-disk config matches expected
- Defaults file holds the canonical §2.4 matrix as Ansible variable

## Acceptance Criteria
- [ ] Role idempotent
- [ ] All 8 per-topic config files present and matching §2.4
- [ ] `ruflo config show --topic <each>` reflects expected flags
- [ ] `validate_matrix.py` exits 0
- [ ] `ansible-lint` passes

## Context Files
- [tech-spec.md](../tech-spec.md) — §2.3 role 7, §2.4 matrix, §3 D17, D18
- [user-spec.md](../user-spec.md) — AC25–AC28

## Verification Steps
**Automated:**
- `molecule test -s ruflo`

**Smoke:**
- After apply: `for t in core mcp wasm demo-client docs loop protocol-qa ops; do ruflo config show --topic $t; done` matches §2.4
- `python ~/.fabric/ruflo/validate_matrix.py` exits 0

## Details
**Files:**
- `infrastructure/ansible/roles/ruflo/{tasks,defaults,templates,files,molecule}/`
- Template: `ruflo/topics/{topic}.yml.j2`

**Edge cases:**
- ruflo upstream installer auto-updates — pin to specific version via env var
- Per-topic toggles must propagate to spawned engines — covered separately by mnemonic-tg-bridge config

**Implementation hints:**
- Keep the matrix as a single YAML variable; template iterates
- `validate_matrix.py` becomes a watchdog check later (T18)

## Reviewers
- code-reviewer → `work/coding-fabric/logs/working/task-10/code-reviewer-{round}.json`
- security-auditor → `work/coding-fabric/logs/working/task-10/security-auditor-{round}.json`

## Post-completion
- [ ] Write report to `work/coding-fabric/decisions.md`
